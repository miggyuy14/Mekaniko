<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.miggy.mekaniko">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <service
            android:name=".onAppKilled"
            android:stopWithTask="false" />

        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CustomerLoginActivity" />
        <activity android:name=".SignUpActivity2" />
        <activity android:name=".SignUpActivity" />
        <activity
            android:name=".CustomerMapActivity"
            android:label="@string/title_activity_customer_map"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MechanicLoginActivity" />
        <activity android:name=".MapActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").


             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MechanicMapActivity"
            android:label="@string/title_activity_mechanic_map"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CustomerSettingsActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MechanicSettingsActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".HistoryActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".HistorySingleActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PasswordResetActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Main2Activity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MessageActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RegisterActivity2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MechChatActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MechMessageActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AdminPanelActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CustomerManagenent"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MechanicManagement"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AdminCustomerHistory"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AdminCutomerSingleHistory"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AdminLogin" />
        <activity android:name=".AdminMehanicHistory" />
        <activity android:name=".AdminMechanicHistorySingle" />
        <activity android:name=".AdminCustomerReq" />
        <activity android:name=".AdminMessageActivity" />
        <activity android:name=".AdminChat"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AdminRegister" />
        <activity android:name=".ChatAdminActivity"></activity>
    </application>

</manifest>